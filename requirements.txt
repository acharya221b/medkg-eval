# --- CORE & CONFLICT RESOLUTION ---
# Pinning scispacy and its STRICTEST dependency.
# This forces everything else to adapt.
scispacy==0.5.3
scipy<1.11

# --- SCISPACY'S OTHER DEPENDENCIES (Manually added, MINUS nmslib) ---
# By adding these here, we tell pip "you must satisfy these" and since
# nmslib is not in the list, pip's resolver has no reason to fetch it.
spacy<3.7.0,>=3.6.0
conllu>=4.4.1
joblib>=0.14.1
pysbd>=0.3.4
scikit-learn>=0.20.3
requests<3.0.0

# --- THE REST OF YOUR PROJECT'S STACK ---
pandas
faiss-cpu
sentence-transformers
torch # Pip will find a compatible CPU version
python-dotenv
openai
tqdm
nebula3-python
llama-index-llms-ollama

# --- SCISPACY MODEL ---
https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.3/en_ner_bionlp13cg_md-0.5.3.tar.gz